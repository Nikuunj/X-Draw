import { SelectShapeType } from '@/actions/canva/Game'
import { ReactNode } from 'react'

function IconTool({ selectedShape, handleClick, children }: { selectedShape: SelectShapeType, handleClick?: () => void, children:  ReactNode })  {
     return (
          <div>IconTool</div>
     )  
}

export default IconTool